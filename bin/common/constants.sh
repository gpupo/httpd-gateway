source "$PWD/.env";
HTTPS_GATEWAY_VAR_DIRECTORY="$PWD/var";
SERVER_STAGE_DIRECTORY="$HOME/stage";
SERVER_STAGE_CONFIG_DIR_PATH="$SERVER_STAGE_DIRECTORY/config";
DOCKER_COMPOSE_BIN_PATH=/usr/local/bin;

test -d $SERVER_STAGE_DIRECTORY || mkdir -p $SERVER_STAGE_DIRECTORY;
test -f $SERVER_STAGE_CONFIG_DIR_PATH || touch $SERVER_STAGE_CONFIG_DIR_PATH;

